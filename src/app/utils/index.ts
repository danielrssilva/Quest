import classNames from './classNames';
import { COLOR_MAPS, FILL_COLOR_MAPS } from './colors';
import getCleanGameName from './game';
import {
  separateNicknameGametag,
  getUserGametag,
  clearSpaces,
} from './gametag';
import {
  BUTTON_BACKGROUND_SIZE_MAPS,
  FONT_SIZE_MAPS,
  ROUND_SIZE_MAPS,
  ROUND_BUTTON_SIZE_MAPS,
  ROUND_BUTTON_BACKGROUND_SIZE_MAPS,
  INPUT_BACKGROUND_SIZE_MAPS,
  BUTTON_SIZE_MAPS,
  SIZE_MAPS,
} from './size';
import getStatusColor, { getFillColor, getTextColor } from './statusColor';

export {
  classNames,
  COLOR_MAPS,
  FILL_COLOR_MAPS,
  getCleanGameName,
  getUserGametag,
  separateNicknameGametag,
  clearSpaces,
  BUTTON_BACKGROUND_SIZE_MAPS,
  ROUND_BUTTON_BACKGROUND_SIZE_MAPS,
  INPUT_BACKGROUND_SIZE_MAPS,
  BUTTON_SIZE_MAPS,
  FONT_SIZE_MAPS,
  ROUND_SIZE_MAPS,
  ROUND_BUTTON_SIZE_MAPS,
  SIZE_MAPS,
  getStatusColor,
  getFillColor,
  getTextColor,
};
